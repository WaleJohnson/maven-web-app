BIN_DIR = bin
SOURCE_FILE = Dockerfile2

all: clean build

clean:
	@echo "Cleaning bin"
	rm -rf $(BIN_DIR)

build:
	@echo "Building this project.."
	mkdir $(BIN_DIR)

copy:
	@echo "Copy to bin"
	cp $(SOURCE-FILE) $(BIN_DIR)

status:
	@echo "Done"

BIN_DIR = bin

all: clean build

clean:
	@echo "Cleaning bin"
	rm -rf $(BIN_DIR)

build:
	@echo "Building this project.."
	mkdir $(BIN_DIR)

copy:
	@echo "Copy to bin"
	cd /maven-web-app
	cp Dockerfile2 /$(BIN_DIR)

status:
	@echo "Done"
